<!DOCTYPE html>

<html>
    <head>
        <title>Timur's Blog</title>
        <link rel="stylesheet" type="text/css" href="/css/mystyle_blog.css"/>
    </head>

    <body>
        <h1><a href="/blog">Timur's Blog</a></h1>
        <div class="logout"><a href="/logout">Log Out</a></div>

        <br>

        {% for post in posts %}
        <div class="post">
            <div class="post-subject">{{post.subject}}</div>
            <div class="post-date">{{post.created.date()}}</div>
	    <hr>
            <pre class="post-content">{{post.content}}</pre>
        </div>
        {% endfor %}
        <br>
        <br>
        <a href="/blog/newpost">New Post</a>

    </body>
</html>
